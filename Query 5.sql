CREATE VIEW branchesview AS
SELECT * FROM branches;

CREATE VIEW departmentsview AS
SELECT * FROM departments;

CREATE VIEW employeesview AS
SELECT * FROM employees;

CREATE VIEW studentsview AS
SELECT * FROM students;

